{# templates/admin/genre/new.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Ajouter un Genre{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center mb-4">Ajouter un Nouveau Genre</h1>

        {{ form_start(form) }}
            {{ form_row(form.nom) }}
            <button type="submit" class="btn btn-success">Ajouter</button>
        {{ form_end(form) }}

        <a href="{{ path('admin_genre_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    </div>
{% endblock %}
